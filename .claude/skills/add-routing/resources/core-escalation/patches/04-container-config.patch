diff --git a/src/container-config.ts b/src/container-config.ts
index 0bb64080..771a56aa 100644
--- a/src/container-config.ts
+++ b/src/container-config.ts
@@ -63,6 +63,12 @@ export interface ContainerConfig {
   maxMessagesPerPrompt?: number;
   model?: string;
   effort?: string;
+  /**
+   * Escalation provider (llm-router §16c). When set, the agent-runner re-runs
+   * a terminally-failed turn ONCE on this provider (fresh session) — covers
+   * the router's no_adequate_model rejection and hard failures alike.
+   */
+  fallbackProvider?: string;
   /**
    * Deliver unwrapped agent prose to the originating room instead of dropping
    * it as scratchpad. Contributed by an installed module (webchat sets it for
@@ -90,6 +96,7 @@ export function configFromDb(row: ContainerConfigRow, group: AgentGroup): Contai
     maxMessagesPerPrompt: row.max_messages_per_prompt ?? undefined,
     model: row.model ?? undefined,
     effort: row.effort ?? undefined,
+    fallbackProvider: row.fallback_provider ?? undefined,
   };
 }
 
